## Spec Registry Update

**Update spec registry**: Verify the feature's entry in `specs/registry.json` (should already exist from the create script). Update the entry's `title` and `summary` fields with the actual feature title and a one-sentence summary extracted from the generated spec. Confirm `status` is `"draft"`. If the registry file or entry is missing, create/add it.
