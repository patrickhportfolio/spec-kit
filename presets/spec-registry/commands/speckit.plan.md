## Spec Registry Update

**Update spec registry**: Update the `status` field in `specs/registry.json` for this feature's entry to `"planned"`. If the registry file or entry does not exist, skip silently.
